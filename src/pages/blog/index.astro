---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

const base = import.meta.env.BASE_URL;

const posts = await getCollection('blog', ({ data }) => !data.draft);
const sorted = posts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<Layout title="Blog — Marissa Joy" description="Writing by Marissa Joy">
  <h1 class="text-3xl font-bold mb-8">Blog</h1>
  <ul class="divide-y divide-gray-100">
    {sorted.map(post => (
      <li class="py-5">
        <a href={`${base}blog/${post.id}`} class="group">
          <h2 class="font-semibold text-lg group-hover:text-gray-600 transition-colors">{post.data.title}</h2>
          <p class="text-sm text-gray-600 mt-1">{post.data.description}</p>
          <div class="flex items-center gap-3 mt-2">
            <time class="text-xs text-gray-400">
              {post.data.pubDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
            </time>
            {post.data.tags.length > 0 && (
              <ul class="flex gap-2">
                {post.data.tags.map(tag => (
                  <li class="text-xs bg-gray-100 text-gray-600 px-2 py-0.5 rounded">{tag}</li>
                ))}
              </ul>
            )}
          </div>
        </a>
      </li>
    ))}
  </ul>
</Layout>
